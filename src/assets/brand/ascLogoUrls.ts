/**
 * ASC wordmark SVGs (vector) — design exports `On_Dark` / `On_White`.
 * Served from `public/assets/brand/asc-logo/`.
 */
export const ascWordmarkSvgUrl = {
  /** Dark chrome (sidebar header above menu). */
  onDark: '/assets/brand/asc-logo/On_Dark.svg',
  /** Light surfaces. */
  onLight: '/assets/brand/asc-logo/On_White.svg',
} as const;
